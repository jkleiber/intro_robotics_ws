#include <ros/ros.h>

/**
 * 
 */
int main(int argc, char **argv)
{
    //Start the node
    ros::init(argc, argv, "mapper");

    //Set up the node handle for auto driving
    ros::NodeHandle mapper_node;
}
